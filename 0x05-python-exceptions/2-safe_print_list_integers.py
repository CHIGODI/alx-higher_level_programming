#!/usr/bin/python3


def safe_print_list_integers(my_list=[], x=0):
    count = 0;
    while count < x:
        print("{:d}".format(mylist[count]))
        count += 1
    except Exception:
        continue

    print()
    return (count)
